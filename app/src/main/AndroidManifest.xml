<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          
    package="com.example.kk.studentdorm"
    >

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".LogIn"></activity>


    <activity android:name=".StudentMessege"></activity>

        <activity android:name=".RenShu"></activity>

        <activity android:name=".Select"></activity>

        <activity android:name=".LiangRen"></activity>

        <activity android:name=".SanRen"></activity>

        <activity android:name=".SiRen"></activity>
        <activity android:name=".Success"></activity>
    </application>

</manifest>
